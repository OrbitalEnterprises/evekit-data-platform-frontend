<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>enterprises.orbital</groupId>
        <artifactId>license</artifactId>
        <version>1.1.0</version>
    </parent>
    <groupId>enterprises.orbital.evekit</groupId>
    <artifactId>data-platform-frontend</artifactId>
    <version>4.0.0</version>
    <packaging>war</packaging>
    <name>EveKit Data Platform Frontend</name>
    <description>Frontend and Web Services for EveKit Data Platform</description>
    <url>https://github.com/OrbitalEnterprises/evekit-data-platform-frontend</url>
    <scm>
        <url>https://github.com/OrbitalEnterprises/evekit-data-platform-frontend</url>
        <connection>scm:git:https://github.com/OrbitalEnterprises/evekit-data-platform-frontend.git</connection>
        <developerConnection>scm:git:git@github.com:OrbitalEnterprises/evekit-data-platform-frontend.git</developerConnection>
    </scm>
    <properties>
        <enterprises.orbital.evekit.dataplatform.appname>evekit-dp</enterprises.orbital.evekit.dataplatform.appname>
        <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
        <enterprises.orbital.evekit.build.timestamp>${maven.build.timestamp}</enterprises.orbital.evekit.build.timestamp>
        <enterprises.orbital.evekit.dataplatform.db.driver>com.mysql.jdbc.Driver</enterprises.orbital.evekit.dataplatform.db.driver>
        <enterprises.orbital.evekit.dataplatform.db.dialect>org.hibernate.dialect.MySQLDialect</enterprises.orbital.evekit.dataplatform.db.dialect>
        <enterprises.orbital.auth.eve_verify_url>https://login.eveonline.com/oauth/verify</enterprises.orbital.auth.eve_verify_url>
        <enterprises.orbital.tokenLimit>50</enterprises.orbital.tokenLimit>
        <orbital.toolchain.version>2.3.0</orbital.toolchain.version>
        <evekit.dataplatform.version>4.0.0</evekit.dataplatform.version>
        <servlet.version>3.1.0</servlet.version>
        <swagger.version>1.5.7</swagger.version>
        <jackson.core.version>2.4.5</jackson.core.version>
        <hsqldb.version>2.3.3</hsqldb.version>
        <mysql.connector.version>5.1.38</mysql.connector.version>
        <log4j.version>1.2.17</log4j.version>
        <glassfish.json.version>1.0.4</glassfish.json.version>
        <javax.json.version>1.0</javax.json.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>enterprises.orbital.evekit</groupId>
            <artifactId>data-platform</artifactId>
            <version>${evekit.dataplatform.version}</version>
        </dependency>
        <dependency>
            <groupId>javax.json</groupId>
            <artifactId>javax.json-api</artifactId>
            <version>${javax.json.version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish</groupId>
            <artifactId>javax.json</artifactId>
            <version>${glassfish.json.version}</version>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>${servlet.version}</version>
        </dependency>
        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-jersey2-jaxrs</artifactId>
            <version>${swagger.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hsqldb</groupId>
            <artifactId>hsqldb</artifactId>
            <version>${hsqldb.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <finalName>evekit-${project.artifactId}-${project.version}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <!-- Make sure we sub in our web configuration -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <webResources>
                        <resource>
                            <filtering>true</filtering>
                            <directory>src/main/webapp</directory>
                            <includes>
                                <include>**/web.xml</include>
                                <include>**/index.html</include>
                            </includes>
                        </resource>
                    </webResources>
                    <warSourceDirectory>src/main/webapp</warSourceDirectory>
                    <webXml>src/main/webapp/WEB-INF/web.xml</webXml>
                </configuration>
            </plugin>
            <!-- This allows you to deploy to a Tomcat server -->
            <!-- Use a profile to create appropriate settings (see README.md) -->
            <plugin>
                <groupId>org.apache.tomcat.maven</groupId>
                <artifactId>tomcat7-maven-plugin</artifactId>
                <version>2.2</version>
                <configuration>
                    <url>${enterprises.orbital.evekit.dataplatform.tomcat.url}</url>
                    <server>${enterprises.orbital.evekit.dataplatform.tomcat.server}</server>
                    <path>${enterprises.orbital.evekit.dataplatform.tomcat.path}</path>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
